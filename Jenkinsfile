pipeline {
    agent any
    stages {
        stage('Run Echo Command') {
            steps {
                echo 'Hello, this is a simple echo command running in Jenkins!'
            }
        }
    }
}
